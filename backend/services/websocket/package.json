{
    "name": "@cashmere-monorepo/backend-service-websocket",
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "test": "sst bind \"vitest run --coverage --no-threads\"",
        "test:watch": "sst bind \"vitest watch --coverage --no-threads\"",
        "test:ui": "sst bind \"vitest watch --ui --coverage --no-threads\"",
        "typecheck": "tsc -noEmit"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.338.0",
        "@cashmere-monorepo/backend-core": "workspace:^",
        "viem": "^0.3.36"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.115",
        "@types/node": "^18.16.14",
        "@vitest/coverage-c8": "^0.31.1",
        "sst": "2.10.2",
        "vitest": "^0.31.1"
    }
}
