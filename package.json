{
  "name": "cashmere-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nx run-many --target=build --all --parallel=4 && sst dev",
    "build": "nx run-many --target=build --all --parallel=4 && sst build",
    "deploy": "nx run-many --target=deploy --all --parallel=4 && sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tsconfig/node18": "^2.0.0",
    "aws-cdk-lib": "2.72.1",
    "constructs": "10.1.156",
    "nx": "16.1.1",
    "typescript": "^5.0.4",
    "sst": "^2.8.5"
  },
  "engines": {
    "node": "^18.16.0"
  },
  "packageManager": "pnpm@8.3.0",
  "workspaces": [
    "shared/*",
    "backend/*"
  ]
}
