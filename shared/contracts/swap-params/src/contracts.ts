import { estimateSwapContract } from './estimateSwap';
import { swapParamsContract } from './swapParams';

/**
 * Export list of all the contracts for the swap param's stack
 */
export const swapParamsApiContracts = {
    estimateSwapContract,
    swapParamsContract,
};
