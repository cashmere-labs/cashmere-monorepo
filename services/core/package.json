{
  "name": "@cashmere-monorepo/core",
  "private": true,
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "deploy": "serverless deploy",
    "deploy-production": "serverless deploy --stage production",
    "deploy-staging": "serverless deploy --stage staging",
    "destroy": "serverless remove",
    "destroy-production": "serverless remove --stage production",
    "destroy-staging": "serverless remove --stage staging",
    "format-check": "prettier --check . --ignore-path ../../.prettierignore",
    "format-fix": "prettier --write . --ignore-path ../../.prettierignore",
    "lint-fix": "pnpm linter-base-config --fix",
    "lint-fix-all": "pnpm lint-fix .",
    "linter-base-config": "eslint --ext=js,ts .",
    "sls-get-region": "serverless print --path provider.region",
    "test": "nx run format-check && nx run test-linter && nx run test-type && nx run test-unit && nx run test-circular",
    "test-circular": "pnpm depcruise --config -- .",
    "test-linter": "pnpm linter-base-config .",
    "test-type": "tsc --noEmit  --emitDeclarationOnly false",
    "test-unit": "vitest run --coverage --passWithNoTests"
  },
  "dependencies": {
    "@cashmere-monorepo/serverless-configuration": "workspace:*",
    "@swarmion/serverless-helpers": "^0.28.1"
  },
  "devDependencies": {
    "@cashmere-monorepo/core-contracts": "workspace:*",
    "@serverless/typescript": "^3.29.0",
    "@types/node": "^18.16.3",
    "@vitest/coverage-c8": "0.30.1",
    "dependency-cruiser": "^12.11.3",
    "esbuild": "^0.17.18",
    "eslint": "^8.39.0",
    "serverless": "^3.30.1",
    "serverless-esbuild": "^1.43.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4",
    "vite-tsconfig-paths": "^4.2.0",
    "vitest": "0.30.1"
  }
}
